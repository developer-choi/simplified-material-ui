# [ComponentName] 컴포넌트

> [단순화된 컴포넌트 한 줄 요약]

---

## 이 문서의 목적

**이 문서는 단순화된 코드의 "설명서"입니다.**

Material-UI는 라이브러리 코드라서 복잡합니다. 단순화했더라도 코드만 보고는 이해하기 어려울 수 있습니다.
이 문서는 코드의 **동작 원리, 핵심 패턴, 왜 이렇게 구현했는지**를 상세히 설명하여 학습을 돕습니다.

> **💡 참고할 예시**: `docs/modal/FocusTrap-simplified.md` - 가장 상세하게 작성된 문서

---

## 무슨 기능을 하는가?

수정된 [ComponentName]는 **[한 줄 정의: 핵심 역할과 책임]** 컴포넌트입니다.

### 핵심 기능 (남은 것)
1. **[기능명 1]** - [설명]
2. **[기능명 2]** - [설명]

> **💡 작성 주의**: 해당 컴포넌트 자체가 하는 일만 작성하세요. 하위 컴포넌트의 기능까지 포함하지 마세요. (예: Dialog에서 Modal이 하는 일인 백드롭/포커스 트랩은 제외)

---

## 핵심 학습 포인트

이 컴포넌트에서 배울 수 있는 **핵심 개념과 패턴**을 코드와 함께 설명합니다.

### 1. [핵심 포인트 1]

```javascript
// 핵심 로직 코드 발췌
```

**학습 가치**:
- [이 코드가 왜 중요한지]
- [어떤 상황에서 이 패턴을 쓸 수 있는지]

### 2. [핵심 포인트 2]

```javascript
// 핵심 로직 코드 발췌
```

**학습 가치**:
- [가치 설명]

> **💡 참고**: `docs/modal/FocusTrap-simplified.md`의 'Sentinel 노드를 이용한 경계 감지', '이벤트 조합을 통한 키 정보 전달' 섹션

---

## 내부 구조

### 1. 렌더링 구조

```javascript
// 위치: packages/mui-material/src/[Path]/[File].js ([N]줄, 원본 [N]줄)

[ComponentName]
  └─> [Child1]  ← [역할 설명]
       └─> [Child2]  ← [역할 설명]
            └─> children
```

### 2. 핵심 상태 (ref, state, 변수)

컴포넌트 내 **ref, state, 변수**의 목적을 테이블로 정리합니다.

| 이름 | 타입 | 용도 |
|------|------|------|
| `someRef` | ref | [한 줄 설명] |
| `someState` | state | [한 줄 설명] |
| `someVar` | 변수 | [한 줄 설명] |

> **💡 참고**: `docs/modal/FocusTrap-simplified.md`의 '핵심 상태' 테이블

### 3. 함수 역할

컴포넌트 내 **모든 함수**의 역할을 상세히 설명합니다.

#### [함수명]()

- **역할**: [한 줄 설명]
- **호출 시점**: [언제 호출되는지]
- **핵심 로직**:

```javascript
// 코드 발췌 및 주석으로 설명
const someFunction = () => {
  // 1. 먼저 X를 체크
  // 2. Y 조건이면 Z 실행
};
```

- **왜 이렇게 구현했는지**: [설계 의도 설명]

> **💡 참고**: `docs/modal/FocusTrap-simplified.md`의 'loopFocus()', 'contain()' 함수 섹션

### 4. 동작 흐름

핵심 로직의 **플로우차트**와 **시나리오 예시**로 동작을 시각화합니다.

#### [핵심 로직명] 플로우차트

```
[이벤트/트리거] 발생
        ↓
┌─────────────────────────────────┐
│ [조건 1] 체크                    │──→ YES → [결과 1]
└─────────────────────────────────┘
        ↓ NO
┌─────────────────────────────────┐
│ [조건 2] 체크                    │──→ YES → [결과 2]
└─────────────────────────────────┘
        ↓ NO
[기본 동작]
```

#### 시나리오 예시

**시나리오 1: [상황 설명]**
```
[사용자 액션] → [내부 동작 1] → [내부 동작 2] → [최종 결과]
```

**시나리오 2: [상황 설명]**
```
[사용자 액션] → [내부 동작] → [결과]
```

> **💡 참고**: `docs/modal/FocusTrap-simplified.md`의 'contain() 플로우차트', 'Tab 순환 시나리오' 섹션

### 5. 핵심 패턴/플래그

특수 목적의 변수나 패턴을 **비유**와 함께 상세히 설명합니다.

#### [플래그/패턴명]

- **비유**: "[직관적인 비유]"
- **역할**: [상세 설명]

**왜 필요한가?**

```javascript
// 플래그 없을 때 문제 상황
[코드 또는 흐름 설명]
```

**플래그가 있으면:**

```javascript
// 플래그로 해결되는 상황
[코드 또는 흐름 설명]
```

> **💡 참고**: `docs/modal/FocusTrap-simplified.md`의 'ignoreNextEnforceFocus 플래그' 섹션

### 6. 주요 변경 사항 (원본 대비)

원본 코드에서 제거/변경된 부분과 그 이유를 설명합니다.

```javascript
// 변경된 코드 스니펫
```

**원본과의 차이**:
- ❌ `[제거된 기능]` 제거 → [대체 동작 설명]
- ❌ `[제거된 prop]` 제거 → [고정값 또는 기본값]
- ✅ `[유지된 기능]` 유지 → [유지 이유]

### 7. Props

| Prop | 타입 | 기본값 | 설명 |
|------|------|--------|------|
| `open` | boolean | - | [설명] |
| `children` | ReactNode | - | [설명] |

**제거된 Props**:
- ❌ `[prop명]` - [제거 이유]
- ❌ `[prop명]` - [제거 이유]

---

## 커밋 히스토리로 보는 단순화 과정

[ComponentName]는 **[N]개의 커밋**을 통해 단순화되었습니다.

> **💡 작성 규칙**:
> - 계획서의 커밋 번호/단계와 반드시 일치
> - 각 커밋의 해시(Hash) 포함
> - **삭제된 코드와 그 이유**를 함께 설명

### 1단계: [단계명]

- `[Commit Hash]` - [Commit Message]

**삭제된 코드**:
```javascript
// 삭제된 코드 발췌
```

**왜 불필요한가**:
- **학습 목적**: [이 기능이 핵심 학습과 무관한 이유]
- **복잡도**: [제거로 인한 코드 감소량, 복잡도 감소]

### 2단계: [단계명]

- `[Commit Hash]` - [Commit Message]

**삭제된 코드**:
```javascript
// 삭제된 코드 발췌
```

**왜 불필요한가**:
- [이유 설명]

> **💡 참고**: `docs/modal/FocusTrap-simplified.md`의 커밋 히스토리 섹션

---

## 원본과의 차이점

| 항목 | 원본 | 수정본 |
|------|------|--------|
| **코드 라인** | [N]줄 | [N]줄 ([N]% 감소) |
| **Props 개수** | [N]개 | [N]개 |
| **[기능 1]** | ✅ | ❌ |
| **[기능 2]** | ✅ | ❌ |
| **[기능 3]** | ✅ 복잡한 구현 | ✅ 단순화 |

---

## 학습 후 다음 단계

[ComponentName]를 이해했다면:

1. **[관련 컴포넌트 1]** - [이 컴포넌트와의 관계, 학습 목적]
2. **[관련 컴포넌트 2]** - [학습 목적]
3. **실전 응용** - [직접 만들어볼 수 있는 예시]

**예시: 기본 사용**
```javascript
// 가장 기본적인 사용법
```

**예시: [응용 사례]**
```javascript
// 응용 예시 코드
```
