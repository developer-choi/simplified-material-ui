# Input 관련 컴포넌트 간소화 순서 계획서

## 1. 컴포넌트 의존성 구조

```
[외부 사용자 레벨]
    ↓
TextField (Input + InputLabel + FormControl 통합)
    ↓
┌───────────────┬──────────────┬───────────────┐
│               │              │               │
Input       FilledInput   OutlinedInput   [기타: Select, Autocomplete 등]
│               │              │
└───────────────┴──────────────┴───────────────┘
                ↓
          InputBase (공통 기반)
                ↓
┌───────────────┴────────────────┐
│                                │
FormControl              TextareaAutosize
(상태 관리)              (multiline 처리)


[독립 컴포넌트들 - Input과 함께 사용됨]
├─ InputAdornment (startAdornment/endAdornment로 전달)
└─ InputLabel (TextField 내부에서 사용)
```

## 2. 의존성 분석 결과

| 컴포넌트 | 코드 라인 | 의존하는 컴포넌트 | 이 컴포넌트를 사용하는 컴포넌트 |
|---------|----------|-----------------|---------------------------|
| **InputAdornment** | 218줄 | Typography, FormControlContext | ❌ 없음 (사용자가 직접 props로 전달) |
| **InputLabel** | 304줄 | FormLabel, FormControlContext | TextField |
| **InputBase** | 848줄 | TextareaAutosize, FormControl | Input, FilledInput, OutlinedInput, Select, Autocomplete 등 |
| **Input** | 380줄 | **InputBase** | TextField, Select, NativeSelect, FormControl 등 |

## 3. 간소화 순서 (자식 → 부모)

### Phase 1: 완전 독립 컴포넌트 (병렬 작업 가능)

#### 1️⃣ InputAdornment (우선순위: 최상)
- **이유**: 아무 컴포넌트도 import 안 함 (가장 독립적)
- **복잡도**: 낮음 (218줄)
- **영향 범위**: 최소 (사용자 코드에만 영향)

#### 2️⃣ InputLabel (우선순위: 상)
- **이유**: TextField만 사용, Input과는 독립적
- **복잡도**: 중간 (304줄)
- **영향 범위**: TextField에만 영향

### Phase 2: 기반 컴포넌트

#### 3️⃣ InputBase (우선순위: 최상 ⭐)
- **이유**: Input의 기반, 가장 복잡한 핵심 컴포넌트
- **복잡도**: 최상 (848줄)
- **영향 범위**: Input, FilledInput, OutlinedInput 등 모두에 영향
- **전략**: InputBase를 간소화하면 Input도 자동으로 간단해짐

### Phase 3: 상위 래퍼 컴포넌트

#### 4️⃣ Input (우선순위: 중)
- **이유**: InputBase를 사용하는 래퍼 (underline 스타일만 추가)
- **복잡도**: 낮음 (380줄, 대부분 스타일)
- **영향 범위**: TextField 등
- **전략**: InputBase 간소화 후 작업하면 훨씬 쉬움

## 4. 추천 작업 순서

### ✅ 최우선 추천: InputBase

**근거:**
1. **최대 효과** - InputBase를 간소화하면 Input, FilledInput, OutlinedInput 모두 혜택
2. **가장 복잡** - 848줄, FormControl 통합, multiline, Slot 시스템 등 모든 복잡도 포함
3. **학습 가치** - Input 컴포넌트의 핵심 로직이 모두 여기 있음
4. **의존성 안전** - InputBase 자체는 자식이므로 간소화해도 다른 컴포넌트 안전

**예상 간소화 비율:** 848줄 → 약 200-300줄 (60-65% 감소)

### 🔄 대안: 작은 것부터 (학습 곡선 고려)

만약 848줄이 부담스러우면:

**순서: 1 → 2 → 3 → 4**

1. **InputAdornment** (218줄) - 워밍업
2. **InputLabel** (304줄) - 중간 난이도
3. **InputBase** (848줄) - 메인 이벤트
4. **Input** (380줄) - 마무리

**장점:** 작은 컴포넌트로 패턴 익히고 → 큰 컴포넌트 도전
**단점:** 시간 더 걸림 (4개 작업)

## 5. 최종 추천

### 🎯 추천: InputBase 단독 집중

**이유:**
- InputAdornment, InputLabel은 Input과 독립적 (나중에 해도 무방)
- InputBase가 핵심이며 가장 복잡
- InputBase 간소화 후 Input은 자동으로 간단해짐
- 학습 목적상 InputBase의 핵심 로직 이해가 최우선

**작업 계획:**
1. **지금:** InputBase 간소화 (Plan Mode 진입)
2. **나중:** Input 간소화 (InputBase 완료 후)
3. **선택:** InputAdornment, InputLabel (필요시)

## 6. 컴포넌트별 주요 간소화 항목 (참고)

### InputBase 예상 간소화 항목
- Slot 시스템 (`slots`, `slotProps`, `components`, `componentsProps`)
- Theme 시스템 (`useDefaultProps`, `useUtilityClasses`, `memoTheme`)
- Styled Components → 인라인 스타일
- FormControl 통합 (단순화)
- multiline/TextareaAutosize (단순 input만)
- size prop (medium 고정)
- color prop (primary 고정)
- margin prop
- readOnly prop
- renderSuffix
- disableInjectingGlobalStyles
- PropTypes 제거

### Input 예상 간소화 항목
- disableUnderline → 항상 underline 표시
- color variants (여러 색상 → 단일 색상)
- 나머지는 InputBase 간소화에 따라 자동 간소화

### InputAdornment 예상 간소화 항목
- variant prop (filled/outlined/standard → 단순화)
- disableTypography
- disablePointerEvents
- FormControl 통합

### InputLabel 예상 간소화 항목
- shrink 애니메이션 (disableAnimation)
- variant별 위치 조정 (standard 고정)
- FormControl 통합 단순화
