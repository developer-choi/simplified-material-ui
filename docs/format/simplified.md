# [ComponentName] 컴포넌트

> [단순화된 컴포넌트 한 줄 요약]

---

## 무슨 기능을 하는가?

수정된 [ComponentName]는 **[한 줄 정의: 핵심 역할과 책임]** 컴포넌트입니다.

### 핵심 기능 (남은 것)
1. **[기능명 1]** - [설명]
2. **[기능명 2]** - [설명]

> **💡 작성 주의**: 해당 컴포넌트 자체가 하는 일만 작성하세요. 하위 컴포넌트의 기능까지 포함하지 마세요. (예: Dialog에서 Modal이 하는 일인 백드롭/포커스 트랩은 제외)  
> *(참고: `docs/modal/Dialog-simplified.md`의 '무슨 기능을 하는가?' 섹션)*

---

## 핵심 학습 포인트
*(예시: `docs/data-display/Avatar-simplified.md`의 '핵심 학습 포인트' 섹션 참고)*

### 1. [핵심 포인트 1]
```javascript
// 핵심 로직 코드
```

**학습 가치**:
- [가치 1]
- [가치 2]

---

## 내부 구조

### 1. 컴포넌트 구조 (또는 렌더링 구조)

```javascript
// 위치: packages/mui-material/src/[Path]/[File].js ([N]줄, 원본 [N]줄)
Component
  └─> Child
       └─> GrandChild
```

### 2. 핵심 상태 (ref, state, 변수)

컴포넌트 내 **ref, state, 변수**의 목적을 테이블로 정리합니다.

| 이름 | 용도 |
|------|------|
| `someRef` | 한 줄 설명 |
| `someState` | 한 줄 설명 |

> **💡 작성 팁**: 모든 ref/state를 나열할 필요 없이, 이해에 핵심적인 것만 정리하세요.
> *(참고: `docs/modal/FocusTrap-simplified.md`의 '핵심 Ref들' 섹션)*

### 3. 함수 역할

컴포넌트 내 **함수** (일반 함수, useCallback, 이벤트 핸들러)의 역할을 설명합니다.

#### [함수명]()
- **역할**: 한 줄 설명
- **호출 시점**: 언제 호출되는지
- **핵심 로직** (필요시):
```javascript
// 코드 발췌
```

> **💡 작성 팁**: 복잡한 함수는 플로우차트나 시나리오 예시로 보충하세요.
> *(참고: `docs/modal/FocusTrap-simplified.md`의 'loopFocus() 함수', 'contain() 함수' 섹션)*

### 4. 동작 흐름 (선택)

핵심 로직의 **플로우차트** 또는 **시나리오 예시**로 동작을 시각화합니다.

```
입력 → 조건1 체크 → 조건2 체크 → 결과
        ↓ NO
      early return
```

**시나리오 예시:**
```
사용자가 X를 함 → Y가 발생 → Z 결과
```

> **💡 작성 팁**: 모든 함수에 필요하지 않습니다. 흐름이 복잡한 경우에만 추가하세요.
> *(참고: `docs/modal/FocusTrap-simplified.md`의 'contain() 함수' 플로우차트)*

### 5. 핵심 패턴/플래그 (선택)

특수 목적의 변수나 패턴을 **비유**로 쉽게 설명합니다.

- `[플래그명]`: "[비유]" - [상세 설명]

> **💡 예시**: `ignoreNextEnforceFocus` = "일회용 패스권" - 다음 한 번만 포커스 강제를 무시
> *(참고: `docs/modal/FocusTrap-simplified.md`의 'ignoreNextEnforceFocus 플래그' 섹션)*

### 6. [주요 변경된 로직/컴포넌트]
[코드 스니펫 및 설명]

> **💡 원본과의 차이를 서술해주세요. (아래 예시)**:
> - ❌ `disableAutoFocus` 제거 → 항상 자동 포커스
> - ❌ `disableRestoreFocus` 제거 → 항상 복원
> *(참고: `docs/modal/FocusTrap-simplified.md`의 '초기 포커스 및 복원' 섹션)*

### 7. Props ([N]개만 남음)

| Prop | 타입 | 기본값 | 설명 |
|------|------|--------|------|
| `open` | boolean | - | ... |

---

## 커밋 히스토리로 보는 단순화 과정

[ComponentName]는 **[N]개의 커밋**을 통해 단순화되었습니다.

> **💡 작성 규칙**:
> - 계획서의 커밋 번호/단계와 반드시 일치시킵니다.
> - 각 커밋의 해시(Hash)를 포함합니다.
> - 시간 순서(또는 단계별 순서)대로 나열합니다.

### 1단계: [단계명]
- `[Commit Hash]` - [Commit Message] ([변경 사항 요약])
- 이 기능이 불필요한 이유 작성 (예시: docs/format/commit.md 의 "왜 불필요한가" 항목 참고)

### 2단계: [단계명]
- `[Commit Hash]` - [Commit Message] ([변경 사항 요약])

---

## 원본과의 차이점

| 항목 | 원본 | 수정본 |
|------|------|--------|
| **코드 라인** | [N]줄 | [N]줄 ([N]% 감소) |
| **Props 개수** | [N]개 | [N]개 |
| **[주요 항목]** | ✅ | ❌ |

---

## 학습 후 다음 단계

[ComponentName]를 이해했다면:

1. **[관련 컴포넌트 1]** - [학습 목적]
2. **[관련 컴포넌트 2]** - [학습 목적]
3. **실전 응용** - [실전 예시 주제]

**예시: [사용 예시 제목]**
```javascript
// 간단한 사용 예시 코드
```

**예시: [커스터마이징 예시 제목]**
```javascript
// 커스터마이징 예시 코드
```
